import { Router } from 'express';
const router = Router();
import {} from '../../controllers/thoughtsController.js';

// need to import thought methods from thoughtController

export default router;
